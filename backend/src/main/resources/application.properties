spring.profiles.active=dev

jwt.header=Authorization
jwt.secret=mySecret
jwt.expiration.seconds=604800
jwt.route.authentication.path=auth
jwt.route.authentication.refresh=refresh

account.creation.path=/auth/create